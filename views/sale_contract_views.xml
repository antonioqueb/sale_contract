<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- LISTA PRINCIPAL -->
        <record id="view_sale_contract_list" model="ir.ui.view">
            <field name="name">sale.contract.list</field>
            <field name="model">sale.contract</field>
            <field name="type">list</field>
            <field name="arch" type="xml">
                <list string="Contratos de venta">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="date_start"/>
                    <field name="date_end"/>
                    <field name="state"/>
                </list>
            </field>
        </record>

        <!-- FORMULARIO -->
        <record id="view_sale_contract_form" model="ir.ui.view">
            <field name="name">sale.contract.form</field>
            <field name="model">sale.contract</field>
            <field name="type">form</field>
            <field name="arch" type="xml">
                <form string="Contrato de venta">
                    <header>
                        <!-- Botones siempre visibles (sin states/attrs) -->
                        <button name="action_confirm" type="object"
                                class="btn-primary" string="Aprobar"/>
                        <button name="action_generate_service" type="object"
                                class="btn-success" string="Generar Servicio"/>
                        <button name="action_generate_excess" type="object"
                                class="btn-secondary" string="Generar Excedente"/>
                        <field name="state" widget="statusbar"
                               statusbar_visible="draft,active,expired,cancelled"/>
                    </header>

                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="partner_id"/>
                            <field name="date_start"/>
                            <field name="date_end"/>
                        </group>

                        <notebook>
                            <page string="Residuos autorizados">
                                <field name="line_ids">
                                    <list editable="bottom">
                                        <field name="product_id"/>
                                        <field name="price_unit"/>
                                        <field name="quantity"/>
                                        <field name="uom_id"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

    </data>
</odoo>
